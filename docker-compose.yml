services:
  front:
    build: .
    image: fsfernando072/familia-connect-front:latest
    container_name: familia-connect-front
    restart: always
    ports:
      - "80:80"
    environment:
      - API_BASE_URL=${API_BASE_URL:-http://localhost:8080/api}
